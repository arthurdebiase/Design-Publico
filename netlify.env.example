# Netlify Environment Variables Template
# Copy these variables to your Netlify site's environment variables in the Netlify Dashboard
# Site settings > Build & deploy > Environment > Environment variables

# Airtable API Configuration (Required)
AIRTABLE_API_KEY=your_airtable_api_key
AIRTABLE_BASE_ID=your_airtable_base_id

# Database Configuration (Optional - only if using a database)
DATABASE_URL=postgresql://username:password@host:port/database

# Email Service Configuration (Optional - only if using email features)
SENDGRID_API_KEY=your_sendgrid_api_key
MAILER_FROM_EMAIL=your_from_email@example.com

# Newsletter Service (Optional - only if using newsletter features)
MAILERLITE_API_KEY=your_mailerlite_api_key

# Node Environment
NODE_ENV=production